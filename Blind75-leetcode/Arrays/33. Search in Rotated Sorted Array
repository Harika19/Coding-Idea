/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var search = function(nums, target) {
 let c=0;
  let s;
    for(let i=0;i<nums.length;i++){
      console.log('cccc', nums[i], target)
        if(nums[i]=== target){
            return i;
        }
    }
 return -1
};
